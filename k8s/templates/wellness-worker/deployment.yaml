apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mind-journal.wellness-worker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: wellness-worker
    {{- include "mind-journal.labels" . | nindent 4 }}
spec:
  replicas: {{ (index .Values "wellness-worker").replicaCount }}
  selector:
    matchLabels:
      app: wellness-worker
  template:
    metadata:
      labels:
        app: wellness-worker
    spec:
      containers:
      - name: wellness-worker
        image: "{{ .Values.global.image.repository }}:{{ .Values.wellness.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - containerPort: {{ (index .Values "wellness-worker").service.httpPort }}
          name: http
        envFrom:
        - configMapRef:
            name: {{ include "mind-journal.wellness-worker.fullname" . }}-config
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: wellness-postgres
              key: password
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: wellness-redis
              key: password
        startupProbe:
          httpGet:
            path: /health/live
            port: http
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          failureThreshold: 3
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          failureThreshold: 3
          periodSeconds: 5
        resources:
          {{- toYaml (index .Values "wellness-worker").resources | nindent 10 }}
